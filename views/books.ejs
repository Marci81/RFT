<% include partials/header %>

<% books.forEach(function (book) { %>
    <div>
        <h4><%= book.title %></h4>
        <img src="<%= book.image %>" alt="">
    </div>
<% }) %>

<% include partials/foter %>
